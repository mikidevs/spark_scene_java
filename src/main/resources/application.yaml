spring:
  datasource:
    url: jdbc:postgresql://localhost:5555/spark_scene_htmx
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    view:
      prefix: /templates/
      suffix: .html
management:
  endpoints:
    web:
      exposure:
        include: mappings
server:
  port: 8088